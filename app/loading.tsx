export default function RootLoading() {
  return (
    <main className="auth-page">
      <section className="auth-card">
        <h1>Loading...</h1>
        <p className="muted">Please wait while the page is being prepared.</p>
      </section>
    </main>
  );
}
